# --- Telegram Bot ---
aiogram==3.13.1
# SOCKS proxy support for the aiogram HTTP session (e.g. via v2rayn)
aiohttp-socks==0.9.0


# --- Database ---
SQLAlchemy[asyncio]==2.0.35
asyncpg==0.30.0

alembic==1.13.3

# --- Redis (FSM storage + Mini App pub/sub) ---
redis==5.1.1

# --- Configuration ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Mini App API + realtime (FastAPI/Starlette + WebSockets) ---
# Reuses the same async SQLAlchemy models/services as the bot; no second ORM.
fastapi==0.115.2
uvicorn[standard]==0.31.1

# --- Logging ---
structlog==24.4.0

# --- Utilities ---
orjson==3.10.7
